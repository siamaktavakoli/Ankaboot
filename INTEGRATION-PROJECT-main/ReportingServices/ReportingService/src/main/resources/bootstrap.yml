CONFIG_HOST: localhost

spring:
  application:
    name: Reporting-Service
  cloud:
    config:
      uri: http://${CONFIG_HOST}:8888

      failFast: true
      retry:
        initialInterval: 3000
        multiplier: 1.3
        maxInterval: 10000
        maxAttempts: 20